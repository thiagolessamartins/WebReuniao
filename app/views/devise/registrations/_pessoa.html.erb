<%= form_for(@user.pessoa) do |f| %>
  <% if @user.pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.pessoa.errors.count, "error") %> prohibited this pessoa from being saved:</h2>

      <ul>
      <% @user.pessoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :cpf %><br>
    <%= f.number_field :cpf %>
  </div>
  <div class="field">
    <%= f.label :uf %><br>
    <%= f.collection_select :uf, Pessoa::UF, :to_s, :upcase %></br>
  </div>
  <div class="field">
    <%= f.label :cidade %><br>
    <%= f.text_field :cidade %>
  </div>
  <div class="field">
    <%= f.label :endereco %><br>
    <%= f.text_field :endereco %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
