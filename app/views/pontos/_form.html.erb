<%= form_for (@ponto) do |f| %>

  <% if @ponto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ponto.errors.count, "error") %> prohibited this ponto from being saved:</h2>

      <ul>
      <% @ponto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_area :titulo %>
  </div>


  <div class="field">


        <%= f.hidden_field 'questao_id' %>

  </div>
  <div class="actions">
    <%= f.submit "Cadastrar Ponto"%>
  </div>
<% end %>
